const addToN = require('./recursion');
const myPow = require('./recursion');



// test('2 raised to the power of 4 should be 16',()=>{
//   expect(myPow(2,4)).toBe(16);
// })

test('add to 4 should be 10',()=>{
  expect(addToN(4)).toBe(10);
})